# Ice King local environment scaffolding
# Copy to `.env` (repo root) or `apps/client/.env` for local development.

# Required for live GPT bot decisions in Play vs Computer mode.
OPENAI_API_KEY=sk-your-openai-api-key

# Optional bot model override.
ICEKING_BOT_MODEL=gpt-5-nano

# Optional fallback model list used when the primary model is unavailable.
ICEKING_BOT_MODEL_FALLBACKS=gpt-5-mini,gpt-4.1-mini

# Optional request timeout (milliseconds) for /api/bot/decide middleware calls.
ICEKING_BOT_TIMEOUT_MS=6500

# Bot mode toggles for client runtime.
# Default behavior is LLM bot enabled; set disable flag to force heuristic fallback.
VITE_ENABLE_LLM_BOT=1
VITE_DISABLE_LLM_BOT=0
